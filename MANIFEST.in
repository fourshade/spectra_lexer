# Root plaintext files (license, changelog, etc.)
include *.txt
# Built-in assets such as rules dictionaries and board graphics.
recursive-include spectra_lexer/assets *
# Built-in SVG resources such as icons.
recursive-include spectra_lexer *.dat
recursive-include spectra_lexer *.svg
# Do not include tests in distribution builds.
exclude test/*
