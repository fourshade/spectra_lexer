# Root plaintext files (license, changelog, etc.)
include *.txt
# Built-in assets such as rules dictionaries and board graphics.
include spectra_lexer/assets/*.*
# setuptools is unpredictable with test scripts - just skip them.
exclude test/*
